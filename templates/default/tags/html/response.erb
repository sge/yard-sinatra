<% if object.has_tag?(:response) %>
  <div class="examples">
    <h3>Response:</h3>
    <% object.tags(:response).each do |tag| %>
      <h4><%= htmlify_line(tag.name) %></h4>
      <pre class="example code source_code"><%= html_syntax_highlight(tag.text, :plain) %></pre>
    <% end %>
  </div>
<% end %>
